# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/) 进行版本迭代。

## 1.5.1 - 2026-08-13

### 新增

- 新增桌宠聊天输入与对话气泡，桌宠会展示 AI 回复内容。
- 新增桌宠漫游设置与右键菜单（回家、隐藏、打开 LaunchPad 等操作）。
- 新增 AI 会话管理：多会话创建、切换与历史记录持久化。
- 新增 AI 记忆管理：小菲会记住主人的偏好与重要信息，跨会话持续生效。

### 优化

- 数据与桌宠模型目录支持迁移，升级后自动保留原有模型与数据。
- 发布说明统一整理到 `release-notes/` 目录，README 补充英文与日文版本。
- 桌宠模型导入支持更多清单变体并加强校验。

## 1.5.0 - 2026-08-13

### 新增

- 新增独立“桌宠工作台”，包含陪伴对话、模型衣橱和桌宠设置三个功能区。
- 新增 Codex v1 / v2 桌宠模型导入，兼容省略 `spriteVersionNumber` 的旧版 v1 清单。
- 新增桌宠多动画状态调度，可展示待机、移动、工作、庆祝、休息与反馈动画。
- 新增 AI 对话能力，内置 OpenAI、DeepSeek、Kimi/月之暗面和智谱 GLM 厂商预设，并支持自定义服务。
- 新增 Chat Completions / Responses API 接口方式选择，以及预设模型和自定义模型 ID。
- 桌宠工作台及相关主进程提示完整支持简体中文、English、日本語。

### 优化

- 严格限制桌宠窗口与拖拽区域，窗口随桌宠位置动态移动，不再持续放大或遮挡大面积桌面。
- 优化对话气泡尺寸和边界处理，避免文本显示不完整。
- 新增导入模型预览图，并修复首次进入模型衣橱时图标不显示的问题。
- AI 回复默认遵循当前界面语言，只有用户明确要求翻译或切换语言时才改变。
- API Key 按厂商分别保存，并由 Electron 主进程通过系统安全存储加密。

### 文档

- 重写 README，补充桌宠模型格式、AI 配置、数据安全与发布流程说明。

## 1.4.6 - 2026-08-13

### 新增

- 一键关闭工作空间：启动台与空间管理卡片在运行中显示「关闭」按钮，按路径结束工作空间内全部进程。
- 新版本提示弹窗：检测到新版本时弹出提示（更新 / 跳过此版本 / 取消），跳过版本不再重复提示。
- 后台静默更新：窗口隐藏于托盘时按「更新方式」自动下载；支持手动、后台静默、全自动三种模式。
- 更新完成后自动启动应用（首次安装仍不自动启动）。

### 优化

- 设置页更新区改为「新版本提醒」开关 + 「更新方式」下拉框两行布局。

## 1.4.5 - 2026-08-13

### 新增

- 诊断与崩溃日志：本地可读 `.txt` 日志 + 一键复制诊断报告（版本、系统、更新错误码、最近错误），方便反馈问题。
- 设置页分类切换：通用 / 更新 / 数据 / 诊断四个标签页，明暗主题自动适配。
- GitHub Actions 自动发布：推送 tag 自动打包 Windows 产物并创建 Release。

## 1.4.4 - 2026-08-12

### 新增

- 新增更新日志弹窗：设置页可查看全部版本发布历史，每个版本一张卡片，支持下载旧版本安装包回滚。
- 开发模式支持手动检查更新（读取 dev-app-update.yml，展示新版本与更新日志预览）。
- 更新日志与 Release Notes 支持 Markdown 渲染，并自动过滤“下载/系统要求”等无关章节。

## 1.4.3 - 2026-08-12

### 优化

- 安装时默认“仅为我安装”（per-user），不再显示“为哪位用户安装”选择页。
- 更新安装完成后不再显示完成向导，直接结束安装进度；任何安装方式都不会默认启动应用。

## 1.4.2 - 2026-08-12

### 新增

- 更新安装完成后展示弹窗，告知用户本次更新内容（Release Notes 预览）。
- 首次安装与更新安装的向导区分：更新时跳过向导页、直接进入安装进度并在原路径更新，默认不启动应用。

### 优化

- 下载完成后由用户点击“重启并安装”，或直接关闭应用时自动安装，不再强制自动重启。
- 用户数据目录跟随当前安装路径（Setup 选择的目录），不再停留在旧配置路径，并自动迁移已有数据。
- 卸载与更新时始终保留用户数据（`LaunchPadData`、userData 与注册表配置）。

## 1.4.1 - 2026-08-12

### 新增

- 新增安装版自动更新检查、下载进度与重启安装能力。
- 新增设置页更新日志预览，检查到新版本时可直接查看 Release Notes。
- 新增安装目录下 `LaunchPadData` 用户数据目录，并在设置页展示实际存储路径。

### 优化

- 应用版本统一从 `package.json` 的运行时版本读取，页面不再维护硬编码版本号。
- `npm install` 自动执行 Electron 原生依赖重建，降低 better-sqlite3 ABI 不匹配风险。

## 1.4.0 - 2026-08-06

### 新增

- 新增系统性能监控面板，实时展示 CPU 使用率、内存占用、磁盘 I/O 与网络吞吐量。
- 新增进程资源排行榜，按 CPU 与内存占用排名，快速识别高负载进程。
- 新增全局快捷键功能，支持自定义快捷键唤起主窗口与快速启动工作空间。
- 新增快捷键占用校验，绑定时自动检测冲突并提示已有绑定者。
- 新增软件图标自动识别模式，自动提取 EXE 图标作为软件库展示。
- 新增软件溢出列表预览，软件数量超出显示区域时以浮层展示完整列表。
- 新增统一确认弹窗组件，覆盖删除、退出、重置等核心操作场景。
- 新增安装版自动更新检查、下载进度与重启安装能力。

### 优化

- 重构工作空间启动引擎，优化启动顺序与状态同步逻辑。
- 重构仪表盘布局，适配性能监控面板并提升信息密度。
- 完善进程管理器交互，优化进程搜索与排序响应速度。
- 升级多语言文案，新增性能监控、快捷键等模块的完整三语翻译。
- 优化软件库界面，支持自定义图标与自动提取图标双模式切换。

### 修复

- 修复多个工作空间同时启动时状态不一致的问题。

## 1.3.0 - 2026-08-04

### 新增

- 新增进程管理控制台，可按应用名称、PID 或监听端口搜索并结束相关进程。
- 新增分页加载与固定分页栏，降低大量 Windows 进程同时渲染造成的卡顿。
- 扫描中心接入 Everything CLI 与 Windows 应用索引，无需预扫描即可直接搜索应用。
- 新增扫描取消能力，支持中止标准、盘符和目录扫描。
- 新增简体中文、English、日本語三语言界面，并覆盖菜单、托盘与运行提示。
- 新增全套 LaunchPad 品牌图标，覆盖窗口、任务栏、托盘、安装包和 EXE 文件。

### 优化

- 扫描结果保存在全局会话中，切换页面、扫描方式或盘符后不再丢失。
- 扫描任务改为异步可中断流程，扫描期间仍可缩小、关闭或操作窗口。
- 改进工作空间运行状态检测，按可执行文件和真实进程信息更新运行状态。
- 进程搜索聚焦应用名称、PID 与端口，移除无效的路径匹配干扰。
- 优化进程列表布局，避免页面和列表同时出现双滚动条。
- 重构全局状态、数据库日志和设置逻辑，提升页面切换的一致性。

### 修复

- 修复软件已经成功启动但工作空间仍显示“已停止”的问题。
- 修复切换扫描界面或盘符后必须重新扫描的问题。
- 修复长时间扫描时无法取消任务的问题。
- 修复 Windows 构建产物仍显示旧 Electron 图标的问题。

## 1.2.0 - 2026-07-31

- 新增系统托盘、开机启动、静默驻留与关闭窗口时驻留托盘。
- 新增启动工作空间前结束已有进程的可选策略。
- 优化启动后自动化脚本执行顺序与工作空间状态监控。

## 1.1.1 - 2026-07-31

- 新增软件添加前启动验证，并改进 EACCES 等启动错误处理。

## 1.1.0 - 2026-07-31

- 新增软件与扫描搜索、BAT/CMD 脚本库、自动化和软件库多种视图。

## 1.0.0 - 2026-07-31

- 首个公开版本：工作空间管理、软件库、批量启动、扫描中心和状态日志。
