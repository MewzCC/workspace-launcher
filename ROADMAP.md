# LaunchPad 路线图

> 面向下一版本的优化与拓展规划。按优先级分组，P0 建议优先进入下个版本。
> 欢迎通过 [Issues](https://github.com/MewzCC/workspace-launcher/issues) 补充与投票。

## P0 · 核心启动体验（建议 v1.4.6）

### 1. 一键关闭工作空间
- 现状：只支持"启动前结束已有进程"（killBeforeLaunch），没有"关闭"能力
- 方案：`workspace:launch` 时记录每个软件启动的 PID（含 ShellExecute 回退路径的进程探测），提供「一键关闭」按钮与托盘菜单入口，按完整 EXE 路径结束工作空间内全部进程

### 2. 启动条件编排
- 端口监听检测：`等 3306 就绪再启动下一项`（连接探测 + 超时提示）
- 进程就绪检测：启动后等待窗口/进程状态稳定再继续
- 失败重试：单软件启动失败自动重试 N 次（可配置）

### 3. 自动备份与恢复
- SQLite 定时备份（每天/每周），保留最近 N 份
- 设置页展示备份列表，支持一键恢复、手动立即备份
- 与现有 JSON 导出互补：JSON 用于跨机迁移，备份用于防损

### 4. 启动日志轮转
- `launch_logs` 目前只增不减，增加 90 天自动清理 + 设置页手动清理

## P1 · 监控增强

### 5. 每进程网络流量
- Windows 计数器/ETW 采集每进程上传/下载速率，进程管理页新增列

### 6. 性能历史持久化
- 当前 CPU/内存/磁盘/GPU 曲线仅内存 60 点、重启即失
- 方案：最近 1 小时数据落 SQLite，重启后恢复曲线

### 7. 资源告警
- 内存/CPU 超阈值时托盘通知（可配置阈值与开关）
- 检测到工作空间内软件异常高占用时提示

## P1 · 更新体系

### 8. 更新渠道切换
- 设置页选择「稳定版 / 测试版」，对应 GitHub prerelease 标志
- 解决 `1.4.5-beta.1` 这类测试版本的分发与回退

### 9. 更新失败诊断增强
- 错误码分类提示（网络/校验/权限/空间不足分别给出中文建议）
- 下载失败自动重试 2 次（退避）

### 10. 代码签名
- 接入 Azure Trusted Signing（月付、无硬件令牌）
- 目标：消除 SmartScreen「未知发布者」提示

## P2 · 体验与工程

### 11. 全局命令面板（Ctrl+K）
- 搜索工作空间/软件/设置项，回车直达，支持「启动 开发环境」自然语言匹配

### 12. 工作空间拖拽排序
- 卡片网格拖拽调整顺序（顺序持久化到数据库）

### 13. 快捷键管理页面
- 集中查看全部绑定、修改、清除、冲突提示

### 14. 渲染层崩溃自愈
- render-process-gone 时自动重载页面并恢复当前视图，避免白屏

### 15. 单元测试补全
- 覆盖：存储路径迁移、版本比较、快捷键校验、数据导入导出 round-trip
- 接入 GitHub Actions 的 test job，PR 自动跑

### 16. 更多自动化能力
- 脚本变量（`{workspace}`, `{appDir}`）、执行条件（仅首次/每次）
- 定时启动工作空间（如每个工作日 9:00）

## 建议节奏

| 版本 | 内容 |
| --- | --- |
| v1.4.6 | 一键关闭 + 启动条件 + 自动备份 + 日志轮转（P0） |
| v1.4.7 | 网络流量 + 性能历史持久化 + 资源告警 + 更新渠道（P1） |
| v1.4.8 | 命令面板 + 拖拽排序 + 快捷键管理 + 崩溃自愈（P2） |
